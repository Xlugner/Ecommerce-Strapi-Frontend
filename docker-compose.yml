version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PUBLIC_STRAPI_URL=https://fresh-actor-bfeb6836b9.strapiapp.com